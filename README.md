# smart-beginning-of-line.el

## introduction

`smart-beginning-of-line.el` provides function to toggle cussor position
between `back-to-indentation' and `beginning-of-line'.

This function expect to replace C-a and home key .
